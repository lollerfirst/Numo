package com.electricdreams.numo.feature.tips

/**
 * Placeholder for TipsManager tests.
 *
 * TipsManager relies on Android SharedPreferences and global singleton
 * state. For now we focus our unit testing effort on pure core models
 * and basket logic; a more isolated prefs abstraction would make this
 * component easier to test deterministically.
 */
class TipsManagerTest
